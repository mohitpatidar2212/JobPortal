# 🧠 Job Portal

A full-featured Job Portal web app where job seekers can create profiles, search and apply for jobs, and employers can post openings, manage applications, and track hiring. Includes authentication, role-based access, and a clean responsive UI for a smooth user experience.

---

## 📂 Project Structure
- **frontend/** – React or HTML/CSS/JS client-side code  
- **backend/** – FastAPI/ backend APIs

---

## 🚀 How to Run
```bash
# Clone the repository
git clone https://github.com/mohitpatidar2212/JobPortal.git

# Navigate into the project
cd JobPortal

# Install dependencies (frontend & backend)
cd frontend && npm install
cd ../backend && npm install

# Start the servers
npm start
